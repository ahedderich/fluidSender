# Risk-acceptance file for the production image Trivy scan (release.yaml).
# Every entry below must have a comment explaining WHY it's accepted and
# which class it belongs to. See SECURITY.md > Container Security for context.
#
# Classes:
#   1) REQUIRED          — a package FluidSender's own code depends on at
#                          runtime, where no patched version has been
#                          released yet. Accepted temporarily because we need
#                          the package and there is nothing to upgrade to —
#                          we will patch as soon as a fixed version becomes
#                          available, not because the CVE is harmless.
#   2) OS-ESSENTIAL       — Debian `Priority: required`/`Essential: yes`
#                          packages that dpkg/apt need to function. apt
#                          refuses to remove these without
#                          --allow-remove-essential, which risks breaking the
#                          image outright. Not removable without switching to
#                          a non-Debian base image (tracked separately).
#
# No findings are currently accepted.
#
# - expr-eval (previously Class 1: CVE-2025-12735, CVE-2025-13204) is gone —
#   replaced by expr-eval-fork, which patches both; see
#   ui/server/utils/macro/macroRunner.ts.
# - udev (previously Class 1) is gone too — serial port enumeration no longer
#   shells out to udevadm (see ui/server/api/serial-ports.get.ts).
# - The old Class 2 OS-essential entries (util-linux family, gzip, libacl1,
#   zlib1g, libtinfo6/ncurses, perl-base) were written against the old
#   node:22-slim root-user runner. Dockerfile.prd's runner stage now ships
#   gcr.io/distroless/nodejs22-debian13:nonroot, which has no apt/dpkg and
#   none of those packages, so the class doesn't apply anymore. Confirm with
#   `trivy image` on the next built image before relying on this — re-add any
#   entry here (with a fresh justification) if the scan still flags it.
